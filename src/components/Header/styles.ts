import styled from 'styled-components';

export const Container = styled.header`
  height: 10vh;
  max-width: 1080px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  z-index: 1;

  img {
    cursor: pointer;
    padding: 42px;
  }
`;
